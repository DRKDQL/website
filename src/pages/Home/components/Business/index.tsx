import Section from "../Section";

const styles = {};

export interface IBusinessProps {}

const Business = (props: IBusinessProps) => {
  return <Section> speakandshinespeechpathology.com.au </Section>;
};

export default Business;
